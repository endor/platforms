This is a basic skeleton for a web app based on node.js.
It was created with the Plat_Forms contest in mind.

Install required ruby dependencies with bundler `bundle install` (see http://gembundler.com/)

Install node dependencies with npm `npm install packetname` (see http://npmjs.org/)
* vows

Start Application with `node server.js`